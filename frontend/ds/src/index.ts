import '../public/assets/styles/main.css'

import type {
   BlockPosition,
   ButtonThemeVariants,
   FormInputType,
   InlinePosition,
   ThemePositions,
   ThemeSizes,
   ThemeType,
   ThemeVariants,
} from './types/global'

import {
   type GroupProps,
   type GroupType,
   DsGroup
} from './components'

export {
    type BlockPosition,
    type ButtonThemeVariants,
    type FormInputType,
    type GroupProps,
    type GroupType,
    type InlinePosition,
    type ThemePositions,
    type ThemeSizes,
    type ThemeType,
    type ThemeVariants,
    DsGroup,
}
