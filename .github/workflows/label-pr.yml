name: Pull Request Lint and Label
on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
jobs:
  pr-request:
    runs-on: ubuntu-latest
    steps:
      - name: Adds Label to PR
        uses: TimonVS/pr-labeler-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
